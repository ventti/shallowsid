// Ionic's global config, read before its components load. A file rather than an
// inline script so the Content-Security-Policy can forbid inline scripts.
window.Ionic = { config: { mode: "ios" } };
